import React from 'react';
import './Spinner.css'; // Ensure you have appropriate styles in this CSS file

const LoadingSpinner = () => (
    <div className="spinner-container">
        <div className="spinner"></div>
    </div>
);

export default LoadingSpinner;
